using UnityEngine;

public enum TaskType
{
    Software,   
    Hardware,   
    HumanResources 
}

public enum TaskDifficulty
{
    Easy,
    Medium,
    Hard
}

public enum GameState
{
    Menu,
    Tutorial,
    NewGameDay,
    DayInProgress,
    EndGameDay,
    EndGame
}